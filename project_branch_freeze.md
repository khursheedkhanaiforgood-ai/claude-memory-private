---
name: feature/traceroute branch freeze
description: feature/traceroute is frozen — no changes allowed without explicit user authorization
type: project
---

The `feature/traceroute` branch on khursheedkhanaiforgood-ai/eve-ng-lab-platform is **frozen**.

**Why:** User confirmed full end-to-end testing passed (extraction, ping, traceroute all working). Branch is stable.

**How to apply:** Do NOT push any commits to `feature/traceroute` without the user explicitly saying "you have my authorization to change feature/traceroute" or equivalent. Ask first every time.

Active development continues on `feature/windows-widget` (branched from `feature/traceroute`), which is for making the agent widget auto-start on Windows.
